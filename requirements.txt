# Cosine Similarity Deep Dive - dependencies
# Pinned to the versions this repo was built and tested against
# (Python 3.11, June 2026). Regenerate with: pip freeze
numpy==2.4.6
scipy==1.17.1
scikit-learn==1.9.0
sentence-transformers==3.4.1
hnswlib==0.8.0
matplotlib==3.10.9
pytest==9.0.3

# Note: sentence-transformers pulls in torch and transformers transitively.
# Verified working with torch==2.12.0, transformers==4.57.6.
